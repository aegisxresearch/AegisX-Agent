[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "utopia-agent"
version = "0.1.0"
description = "Utopia Agent — Super-powered Agentic AI with full tool calling, RAG, planning, memory, and code generation"
readme = "README.md"
license = "MIT"
requires-python = ">=3.10"
authors = [{ name = "Utopia" }]

dependencies = [
    "openai>=1.50.0",
    "anthropic>=0.40.0",
    "rich>=13.0.0",
    "typer>=0.12.0",
    "pydantic>=2.0.0",
    "pydantic-settings>=2.0.0",
    "chromadb>=0.5.0",
    "tiktoken>=0.8.0",
    "aiohttp>=3.10.0",
    "httpx>=0.27.0",
    "pypdf>=4.0.0",
    "python-dotenv>=1.0.0",
    "platformdirs>=4.0.0",
    "numpy>=1.26.0",
    "asyncpg>=0.29.0",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.0.0",
    "pytest-asyncio>=0.23.0",
    "ruff>=0.6.0",
    "mypy>=1.10.0",
]

[project.scripts]
utopia = "utopia_agent.cli:app"

[tool.ruff]
target-version = "py310"
line-length = 100

[tool.ruff.lint]
select = ["E", "F", "I", "N", "W", "UP"]

[tool.mypy]
python_version = "3.10"
strict = true

[tool.hatch.build.targets.wheel]
packages = ["src/utopia_agent"]
